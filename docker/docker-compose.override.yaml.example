# Local Development Override (optional)
#
# For building from local repos, prefer using: make run-build
# This file is only needed for custom overrides beyond what docker-compose.build.yaml provides.
#
# Usage:
#   cp docker-compose.override.yaml.example docker-compose.override.yaml
#   # Edit as needed
#   docker compose up -d
#
# The override file is automatically merged with docker-compose.yml
# See: https://docs.docker.com/compose/extends/

services:
  # Example: override block-builder with custom build args
  # block-builder:
  #   build:
  #     context: ../../blockbuilder
  #     dockerfile: Dockerfile
  #     args:
  #       - BUILD_VERSION=dev

  # Example: override load-generator environment
  # load-generator:
  #   environment:
  #     - DEBUG=true
