#!/bin/bash
# Pre-commit hook: runs test suite before allowing commits
# Install: make setup-hooks

set -e

echo "Running pre-commit checks..."

PROJECT_ROOT=$(git rev-parse --show-toplevel)
cd "$PROJECT_ROOT"

make test

echo ""
echo "All pre-commit checks passed!"
echo ""
